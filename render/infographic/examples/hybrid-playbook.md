---
title: The Canvas
footer: Measured off render.py
---

:: hybrid-playbook
headline: One canvas, **fixed at 4:5**
sub: The frame is not a parameter. Everything else about a render is.
stats:
- 4:5 | the only aspect ratio
- 2x | device scale factor
- 1 | PNG per run
items:
- One layout block | A spec carrying two blocks is a validation error. One canvas holds one claim.
- Structure and tokens split | The stylesheet holds no colour. The profile holds every colour.
- Python owns the geometry | Trapezoids, rings and leader lines are computed, so edges interlock exactly.
- Chrome owns the paint | Headless, no flags a user has to set, nothing to install.
